{
  "name": "kavach-epms-root",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --include=optional && npm run build && cd ../backend && npm install --include=optional"
  }
}
